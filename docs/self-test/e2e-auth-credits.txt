端到端自检 · 账号体系 + 积分系统  http://127.0.0.1:8080  (run=muf7c6yqzhx)

1. 注册与邮箱验证
  ✓ 弱密码被拒（纯字母） — HTTP 400 / VALIDATION_FAILED
  ✓ 弱密码被拒（纯数字） — HTTP 400 / VALIDATION_FAILED
  ✓ 非法用户名被拒 — HTTP 400 / VALIDATION_FAILED
  ✓ 非法邮箱被拒 — HTTP 400 / VALIDATION_FAILED
  ✓ 注册成功返回 201 — HTTP 201
  ✓ 注册直接下发 access token
  ✓ 注册直接下发 refresh token
  ✓ access 有效期 2 小时 — 7200s
  ✓ refresh 有效期 30 天 — 2592000s
  ✓ 新账号邮箱未验证
  ✓ dev 邮件通道回显验证令牌
  ✓ 注册即赠送积分 — 300 分
  ✓ 用户名重名被拒 — HTTP 409 / CONFLICT
  ✓ 邮箱重复被拒 — HTTP 409 / EMAIL_TAKEN
  ✓ 邮箱大小写归一（同一个邮箱） — HTTP 409 / EMAIL_TAKEN
  ✓ me 返回邮箱未验证
  ✓ me 带上余额 — 300 分
  ✓ 邮箱验证成功 — HTTP 200
  ✓ 同一个验证令牌不能用两次 — HTTP 401 / TOKEN_INVALID
  ✓ 伪造的验证令牌被拒 — HTTP 401 / TOKEN_INVALID
  ✓ me 显示邮箱已验证
  ✓ 已验证后重发被拒（不是静默成功） — HTTP 400 / VALIDATION_FAILED
  ✓ 用用户名登录成功 — HTTP 200
  ✓ 用邮箱也能登录同一个账号

2. 防用户名枚举
  ✓ 用户不存在的错误码与密码错误一致 — both 401/UNAUTHORIZED
  ✓ 错误文案不暴露账号是否存在 — "用户名或密码错误"

3. 登录失败锁定
  ✓ 第 1 次失败仍是 401（还剩 4 次） — "用户名或密码错误（还可尝试 4 次）"
  ✓ 第 2 次失败仍是 401（还剩 3 次） — "用户名或密码错误（还可尝试 3 次）"
  ✓ 第 3 次失败仍是 401（还剩 2 次） — "用户名或密码错误（还可尝试 2 次）"
  ✓ 第 4 次失败仍是 401（还剩 1 次） — "用户名或密码错误（还可尝试 1 次）"
  ✓ 第 5 次失败后账号锁定 — HTTP 423 / ACCOUNT_LOCKED
  ✓ 锁定期内密码正确也进不去 — HTTP 423 / ACCOUNT_LOCKED
  ✓ 锁定提示里给出了等待时长 — "连续登录失败次数过多，账号已锁定，请在 15 分钟后重试"

4. 刷新令牌轮换与重放检测
  ✓ 刷新成功换到新令牌
  ✓ 刷新会轮换 refresh（旧的不复用） — 新的 refresh 与旧的不同
  ✓ 重放已用过的 refresh 被拒 — HTTP 401 / TOKEN_INVALID
  ✓ 重放触发全量吊销（连轮换后的新令牌一起作废） — HTTP 401 / TOKEN_INVALID
  ✓ access 是无状态 JWT：吊销会话后短期内仍可用（2h 内自然过期） — HTTP 200

5. 找回密码（含防枚举与冷却）
  ✓ 未注册邮箱也返回「已发送」（防枚举）
  ✓ 未注册邮箱会打码 — "no***@example.com"
  ✓ 已注册邮箱发出重置邮件
  ✓ 冷却期内不重发（但对外的回答不变） — 第二次没有新令牌
  ✓ 垃圾令牌重置被拒 — HTTP 401 / TOKEN_INVALID
  ✓ 重置时弱密码被拒 — HTTP 400 / VALIDATION_FAILED
  ✓ 重置密码成功 — HTTP 200
  ✓ 新密码可以登录
  ✓ 旧密码不再可用
  ✓ 重置后旧 refresh 一并作废 — HTTP 401 / TOKEN_INVALID

6. 登录状态下改密码
  ✓ 原密码错误返回 401 而不是掉线 — HTTP 401 / UNAUTHORIZED
  ✓ 原密码错后原 access 仍然可用（没有被误判为过期）
  ✓ 新旧密码相同时被拒 — HTTP 400 / VALIDATION_FAILED
  ✓ 改密成功并返回新的令牌对
  ✓ 当前设备换到全新 refresh
  ✓ 其他设备被踢下线 — HTTP 401 / TOKEN_INVALID
  ✓ 已作废令牌被重试不会连坐在用的设备（否则就是个免鉴权的踢人开关） — HTTP 200
  ✓ 当前设备无需重新登录

7. 登录设备管理
  ✓ 能列出有效登录设备 — 2 台
  ✓ 设备信息包含可识别的摘要 — "接口客户端 · 未知系统"
  ✓ 设备列表不含令牌本身
  ✓ 可以注销单台设备 — HTTP 200
  ✓ 注销后设备数减少 — 2 → 1

8. 积分账户、套餐与订单
  ✓ 积分概览可读
  ✓ 注册赠送已入账 — 获得 300 / 余额 300
  ✓ 累计消耗为 0
  ✓ 概览带上定价说明 — "每 1000 输入 token 消耗 1 分，每 1000 输出 token 消耗 3 分；单轮最低 1 分。"
  ✓ 流水里有一笔注册赠送 — 1 条
  ✓ 流水带上变动后余额
  ✓ 缺少令牌时重发被拒（该端点需要登录） — HTTP 401 / UNAUTHORIZED
  ✓ 分页上限被夹住（limit=9999 最多回 100 条） — 1 条
  ✓ 余额与账本累计值一致（对账通过） — 余额 300 ｜ 账本 300
  ✓ 套餐列表非空 — 3 档
  ✓ 套餐由后端算好「总积分」与「每千分单价」 — 体验包：1000 分 / 900 分每元
  ✓ 不存在的套餐下单被拒 — HTTP 404 / NOT_FOUND
  ✓ 下单成功 — HTTP 200
  ✓ 下单返回支付参数（payToken）
  ✓ 返回体里的订单不含 payToken（凭证只在下单响应里出现）
  ✓ 待支付订单可以重新取支付参数（真实通道会过期）
  ✓ 重新取参数会换一份新凭证
  ✓ 支付回调成功 — HTTP 200
  ✓ 积分按套餐额度到账 — 300 → 1300（+1000）
  ✓ 重复回调不重复到账（幂等） — 余额仍为 1300
  ✓ 账本里这笔充值只有一条 — 1 条
  ✓ 已支付订单不能再重新取参数 — HTTP 409 / ORDER_NOT_PAYABLE
  ✓ 待支付订单可取消
  ✓ 已取消的订单不能支付 — HTTP 409 / ORDER_NOT_PAYABLE
  ✓ 取消后余额不变

9. 对话链路的预扣与结算
  ✓ 建好自检工作区 — HTTP 201
  ✓ 建好对话会话
  ✓ 发消息被接受（202：立即返回，回答走 SSE） — HTTP 202
  ✓ 一轮对话正常结束 — messageId=95
  ✓ 回答里带上了本轮扣费 — 3 分
  ✓ 回答里带上了扣费后余额 — meta 1297 ｜ 实际 1297
  ✓ 余额确实减少了 — 1300 → 1297
  ✓ 单轮扣费不低于最低消费（1 分） — 3 分
  ✓ 账本里有本轮的预扣（HOLD） — -30 分
  ✓ 账本里有本轮的结算（SETTLE） — 27 分
  ✓ 预扣 + 结算 = 实际扣费 — -30 + 27 = -3
  ✓ 扣费后账本仍然对得上 — 余额 1297 ｜ 账本 1297

10. 余额不足与人工调整（管理端）
  ✓ 引导配置把 e2e_admin 提为管理员 — role=ADMIN
  ✓ 管理员可以人工调整积分 — 余额 0
  ✓ 余额不足时对话被拒（402 而不是 403） — HTTP 402 / INSUFFICIENT_CREDITS
  ✓ 拒绝文案里给出了当前余额与最低需求 — "积分不足（当前 0 分，单轮最低需要 1 分），充值后即可继续"
  ✓ 被拒的请求不会留下一条永远等不到回答的用户消息 — 2 → 2
  ✓ 补回余额成功 — 500 分
  ✓ 管理端能查账号与账本一致性 — 余额 500 ｜ 账本 500

11. 越权与鉴权
  ✓ 非管理员访问管理端被拒 — HTTP 403 / FORBIDDEN
  ✓ 非管理员不能给自己加分 — HTTP 403 / FORBIDDEN
  ✓ 无令牌访问积分接口被拒 — HTTP 401 / UNAUTHORIZED
  ✓ 伪造令牌被拒 — HTTP 401 / UNAUTHORIZED
  ✓ 不能支付别人的订单 — HTTP 404 / NOT_FOUND
  ✓ 订单列表只包含自己的订单 — 0 条

12. 退出登录
  ✓ 退出登录成功 — HTTP 200
  ✓ 退出后 refresh 立即失效 — HTTP 401 / TOKEN_INVALID
  ✓ 退出不需要带上 access（access 往往已过期） — 仅凭 refresh 即可

结果  通过 114  失败 0
